# wallpaper from wikiart artwork of day
#exec_always wikiart-wallpaper >> ~/.config/sway/wallpaper.log 2>&1

# open terminal on workspace 2
#exec swaymsg workspace 2; exec foot --working-directory $HOME

# dbus
#exec --no-startup-id dbus-daemon --session --address=unix:path=$XDG_RUNTIME_DIR/bus

exec_always ~/.config/sway/systemd/wait-sni-ready && \
    systemctl --user start sway-xdg-autostart.target

# notification daemon
exec mako

#kdeconnect
exec QT_QPA_PLATFORM=xcb kdeconnectd
exec --no-startup-id kdeconnectd 
